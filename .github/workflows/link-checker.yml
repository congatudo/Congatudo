on:
  schedule:
    - cron: 6 0 * * * # run daily at 6 am
  workflow_dispatch: # run manually

name: Broken Link Check
jobs:
  check:
    name: Broken Link Check
    runs-on: ubuntu-latest
    steps:
      - name: Broken Link Check
        uses: technote-space/broken-link-checker-action@v2